---
title: Sudoku!
description: 'an algorithm that completes sudokus'
date: January 31 2023
---

##a little introduction, good morning good morning

---

## Problem Solving

##Sudoku explanation and then we come to the code

## Code implemented in python

```
def solve_sudoku(arr, recursive_calls):
    recursive_calls += 1
    for row_i in range(len(arr)):
        for col_i in range(len(arr)):
            if arr[row_i][col_i] == 0:
                for num in range(1, 10):
                    if is_num_possible(row_i, col_i, num, arr):
                        arr[row_i][col_i] = num
                        solve_sudoku(arr, recursive_calls)
                        arr[row_i][col_i] = 0
                return
    print("Solved Sudoku:")
    print_sudoku(arr)
    print("It took " + str(recursive_calls) + " calls")
```


